[tool.poetry]
name = "hiremind-auth"
version = "0.1.0"
description = "Authentication middleware and validation context for HireMind AI"
authors = ["HireMind AI Engineering <engineering@hiremind.ai>"]

[tool.poetry.dependencies]
python = "^3.12"
libs-security = { path = "../security", develop = true }
libs-config = { path = "../config", develop = true }

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
